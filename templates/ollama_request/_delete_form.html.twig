<form method="post" action="{{ path('app_ollama_request_delete', {'id': ollama_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ollama_request.id) }}">
    <button class="btn">Delete</button>
</form>
