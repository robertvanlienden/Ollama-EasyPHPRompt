{% extends 'base.html.twig' %}

{% block title %}PromptType{% endblock %}

{% block body %}
    <h1>PromptType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prompt_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prompt_type.name }}</td>
            </tr>
            <tr>
                <th>PromptPrefix</th>
                <td>{{ prompt_type.promptPrefix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prompt_type_index') }}">back to list</a>

    <a href="{{ path('app_prompt_type_edit', {'id': prompt_type.id}) }}">edit</a>

    {{ include('prompt_type/_delete_form.html.twig') }}
{% endblock %}
